package com.br.pizzaria.domain.entity;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Testes unitários da entidade Pedido (sem Spring context).
 */
class PedidoTest {

    @Test
    @DisplayName("Deve criar Pedido com status RECEBIDO ao instanciar")
    void deveCriarPedidoComStatusRecebido() {
        // TODO: implementar após construtor da entidade
        assertThat(true).isTrue(); // placeholder
    }
}
